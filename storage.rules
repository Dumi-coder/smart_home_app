rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access to all files so the app can show floor plans
      allow read: if true;
      // Require authentication to upload new files
      allow write: if request.auth != null;
    }
  }
}
